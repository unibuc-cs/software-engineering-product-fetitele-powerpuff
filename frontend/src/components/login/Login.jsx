import React from "react";
import './login.css'

function Login(props) {
    console.log(props.name);
    return (
        <div>
            <p>login</p>
        </div>
    );
}

export default Login;